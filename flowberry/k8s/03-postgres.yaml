apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: flowberry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: flowberry
            - name: POSTGRES_USER
              value: flowberry
            - name: POSTGRES_PASSWORD
              value: flowberry
          volumeMounts:
            - name: pg-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: pg-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: flowberry
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
